# No whitespace after the comma or in your part name!
config SHIELD_SPLITFISH_LEFT
    def_bool $(shields_list_contains,splitFish_left)

# No whitespace after the comma or in your part name!
config SHIELD_SPLITFISH_RIGHT
    def_bool $(shields_list_contains,splitFish_right)
    

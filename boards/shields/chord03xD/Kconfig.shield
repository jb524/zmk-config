# Required files for ZMK to recognize the shield

# No whitespace after the comma or in your keyboard name!
# Config name will be reused in Kconfig.defconfig
config CHORD03XD
    def_bool $(shields_list_contains,chord03xD)
